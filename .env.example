# Copiar a .env y completar con los datos del proyecto de Supabase
# (Project settings → API). La anon key es pública: la protección real
# la dan las policies de Row Level Security, no el secreto de la clave.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
